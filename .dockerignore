# Keeps the image build context small and reproducible.

.git
.idea
*.iml

# Maven build output (the image rebuilds it inside the container)
target/
logs/

# Frontend build output & tooling (rebuilt inside the container by frontend-maven-plugin)
web/node_modules/
web/node/
web/dist/

# Secrets must never enter an image layer
.env
